import React, { useEffect, useState, useContext } from 'react';import { useDispatch } from 'react-redux';import { Alert, Modal, Button } from 'react-bootstrap';
import { useNavigate } from 'react-router-dom';
import ProductCard from '../components/ProductCard';
import HeroBanner from '../components/HeroBanner';
import { clearCart } from '../redux/cartSlice';import { AuthContext } from '../context/AuthContext';const Home = () => {  const [products, setProducts] = useState([]);  const [loading, setLoading] = useState(true);  const [paymentSuccess, setPaymentSuccess] = useState(false);  const [paymentError, setPaymentError] = useState(null);  const [orderDetails, setOrderDetails] = useState(null);  const [isConfirming, setIsConfirming] = useState(false);  const { user } = useContext(AuthContext);  const dispatch = useDispatch();  const navigate = useNavigate();  useEffect(() => {    const fetchProducts = async () => {      try {        const res = await fetch('/api/products');        const data = await res.json();        setProducts(data.slice(0, 4));      } catch (error) {        console.error(error);      } finally {        setLoading(false);      }    };    fetchProducts();  }, []);  useEffect(() => {    const params = new URLSearchParams(window.location.search);    const tracker = params.get('tracker');    const order_id = params.get('order_id') || params.get('reference');    const sig = params.get('sig');    if (tracker && order_id) {      const verifyPayment = async () => {        if (!user) return; 
        setIsConfirming(true);        try {          console.log("Verifying payment with params:", { tracker, reference: order_id, sig });          const res = await fetch('/api/payments/confirm-payment', {            method: 'POST',            headers: {              'Content-Type': 'application/json',              'Authorization': `Bearer ${user.token}`            },            body: JSON.stringify({ tracker, reference: order_id, sig })          });          const data = await res.json();          console.log("Verify payment response:", res.status, data);          if (res.ok) {            dispatch(clearCart());            setOrderDetails(data.order || data); 
            setPaymentSuccess(true);            window.history.replaceState({}, document.title, window.location.pathname);          } else {            setPaymentError(data.message || 'Payment confirmation failed');          }        } catch (error) {          console.error("Payment confirmation error:", error);          setPaymentError("An error occurred while confirming your payment.");        } finally {          setIsConfirming(false);        }      };      if (!isConfirming && !paymentSuccess && !paymentError) {        verifyPayment();      }    }  }, [user, dispatch, isConfirming, paymentSuccess, paymentError]);  return (    <div className="home-container">      {isConfirming && (        <Alert variant="info" style={{ maxWidth: '1000px', margin: '20px auto' }}>          Please wait while we verify your payment with Safepay...        </Alert>      )}      {paymentError && (        <Alert variant="danger" onClose={() => setPaymentError(null)} dismissible style={{ maxWidth: '1000px', margin: '20px auto' }}>          {paymentError}        </Alert>      )}      {}      <Modal show={paymentSuccess} onHide={() => setPaymentSuccess(false)} centered size="lg" contentClassName="bg-dark text-white border-secondary">        <Modal.Header closeButton closeVariant="white" className="border-secondary" style={{ backgroundColor: '#10b981', color: '#fff', borderBottom: 'none' }}>          <Modal.Title className="w-100 text-center" style={{ fontWeight: 'bold' }}>🎉 Payment Successful!</Modal.Title>        </Modal.Header>        <Modal.Body style={{ padding: '30px' }}>          {orderDetails ? (            <div>              <p style={{ fontSize: '1.1rem', marginBottom: '15px' }}>Thank you for shopping with VemiroNest, <strong>{user?.name}</strong>!</p>              <p style={{ color: '#a1a1aa', marginBottom: '25px' }}>Your order has been successfully placed and is now being processed.</p>              <div style={{ background: '#09090b', padding: '20px', borderRadius: '12px', border: '1px solid #27272a', marginBottom: '20px' }}>                <h4 style={{ color: '#10b981', marginBottom: '15px' }}>Order Details</h4>                <p style={{ margin: '5px 0' }}><strong>Order ID:</strong> {orderDetails._id}</p>                <p style={{ margin: '5px 0' }}><strong>Total Amount Paid:</strong> Rs. {orderDetails.totalAmount?.toFixed(2)}</p>                <h5 style={{ color: '#f97316', marginTop: '20px', marginBottom: '10px' }}>Items Purchased</h5>                <ul style={{ listStyle: 'none', padding: 0 }}>                  {orderDetails.products && orderDetails.products.map(item => (                    <li key={item._id} style={{ display: 'flex', justifyContent: 'space-between', borderBottom: '1px solid rgba(255,255,255,0.05)', padding: '10px 0' }}>                      <span>{item.qty}x {item.product?.name || 'Item'}</span>                      <span>Rs. {(item.price * item.qty).toFixed(2)}</span>                    </li>                  ))}                </ul>              </div>            </div>          ) : (            <p>Loading your order details...</p>          )}        </Modal.Body>        <Modal.Footer className="border-secondary">          <Button variant="outline-light" onClick={() => navigate('/profile')}>            View My Profile          </Button>          <Button variant="success" onClick={() => setPaymentSuccess(false)} style={{ background: '#10b981', border: 'none' }}>            Continue Shopping          </Button>        </Modal.Footer>
      </Modal>

      <HeroBanner />

      <h2 style={{ textAlign: 'center', marginBottom: '40px', fontSize: '2.5rem' }}>Featured Products</h2>
      {loading ? (        <div>Loading...</div>      ) : (        <div className="product-grid">          {products.map((product) => (            <ProductCard key={product._id} product={product} />          ))}        </div>      )}    </div>  );};export default Home;