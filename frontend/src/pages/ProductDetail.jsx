import React, { useEffect, useState } from 'react';import { useParams, Link } from 'react-router-dom';import { useDispatch } from 'react-redux';import { addToCart } from '../redux/cartSlice';import { Alert } from 'react-bootstrap';import '../styles/product.css';const ProductDetail = () => {  const { id } = useParams();  const [product, setProduct] = useState(null);  const [loading, setLoading] = useState(true);  const [qty, setQty] = useState(1);  const [showAlert, setShowAlert] = useState(false);  const dispatch = useDispatch();  useEffect(() => {    const fetchProduct = async () => {      try {        const res = await fetch(`/api/products/${id}`);        const data = await res.json();        if (res.ok) {          setProduct(data);        } else {          setProduct(null);        }      } catch (error) {        console.error(error);        setProduct(null);      } finally {        setLoading(false);      }    };    fetchProduct();  }, [id]);  const handleAddToCart = () => {    if (product && product.stock > 0) {      dispatch(addToCart({        productId: product._id,        name: product.name,        price: product.price,        imageUrl: product.imageUrl,        qty: qty      }));      setShowAlert(true);      setTimeout(() => setShowAlert(false), 3000);    }  };  if (loading) return <div style={{ textAlign: 'center', margin: '100px', color: '#f97316' }}>Loading Product...</div>;  if (!product) return <div style={{ textAlign: 'center', margin: '100px', color: '#ef4444' }}>Product Not Found</div>;  return (    <div className="product-detail-wrapper" style={{ maxWidth: '1200px', margin: '0 auto', padding: '20px' }}>      {}      <div style={{ color: '#a1a1aa', marginBottom: '20px', fontSize: '0.95rem' }}>        <Link to="/" style={{ color: '#f97316' }}>Home</Link> / <Link to="/shop" style={{ color: '#f97316' }}>Shop</Link> / {product.category} / <span style={{ color: '#fff' }}>{product.name}</span>      </div>      <div className="product-detail">        {}        <div className="detail-image-container">          <img src={product.imageUrl} alt={product.name} className="detail-image" />        </div>        {}        <div className="detail-info">          <h2 style={{ fontSize: '2.8rem', marginBottom: '10px' }}>{product.name}</h2>          <p className="detail-price" style={{ fontSize: '2.5rem', margin: '15px 0' }}>Rs. {Number(product.price).toFixed(2)}</p>          {}          <div style={{ marginBottom: '25px' }}>            <h4 style={{ color: '#fff', marginBottom: '10px' }}>Product Description</h4>            <p style={{ color: '#a1a1aa', lineHeight: '1.8' }}>{product.description}</p>          </div>          {}          <div style={{ display: 'flex', alignItems: 'stretch', gap: '20px' }}>            <div style={{ display: 'flex', alignItems: 'center', background: '#09090b', border: '1px solid #27272a', borderRadius: '8px', overflow: 'hidden' }}>              <button                 onClick={() => setQty(qty > 1 ? qty - 1 : 1)}                style={{ background: 'transparent', border: 'none', color: '#fff', padding: '15px 20px', cursor: 'pointer', fontSize: '1.2rem' }}                disabled={product.stock === 0}              >                -              </button>              <span style={{ color: '#fff', padding: '0 15px', fontSize: '1.2rem', fontWeight: 'bold' }}>{qty}</span>              <button                 onClick={() => setQty(qty < product.stock ? qty + 1 : product.stock)}                style={{ background: 'transparent', border: 'none', color: '#fff', padding: '15px 20px', cursor: 'pointer', fontSize: '1.2rem' }}                disabled={product.stock === 0}              >                +              </button>            </div>            <button onClick={handleAddToCart} className="btn" style={{ flexGrow: '1', display: 'flex', alignItems: 'center', justifyContent: 'center', padding: '10px 20px', fontSize: '1.2rem', opacity: product.stock === 0 ? 0.5 : 1 }} disabled={product.stock === 0}>              Add to Shopping Cart            </button>          </div>          {showAlert && (            <Alert variant="success" style={{ marginTop: '20px', background: 'rgba(16,185,129,0.1)', color: '#10b981', border: '1px solid rgba(16,185,129,0.2)' }}>              Successfully added {qty} {qty > 1 ? 'items' : 'item'} to your cart!            </Alert>          )}          <p style={{ marginTop: '20px', color: product.stock > 0 ? '#10b981' : '#ef4444', fontWeight: '600' }}>            {product.stock > 0 ? `● In Stock (${product.stock} units available)` : `● Temporarily Out of Stock`}          </p>        </div>      </div>    </div>  );};export default ProductDetail;