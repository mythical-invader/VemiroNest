const express = require("express");const { createPaymentSession, paymentWebhook, confirmPayment } = require("../controllers/paymentController");const { optionalAuth } = require("../middleware/authMiddleware");const router = express.Router();router.post("/webhook", paymentWebhook);router.post("/create-session", optionalAuth,  createPaymentSession);router.post("/confirm-payment", optionalAuth, confirmPayment);module.exports = router;